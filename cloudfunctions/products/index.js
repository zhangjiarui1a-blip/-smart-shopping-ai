const cloudbase=require('@cloudbase/node-sdk');const app=cloudbase.init({});const db=app.database();const reply=(statusCode,data)=>({statusCode,headers:{'Content-Type':'application/json; charset=utf-8','Access-Control-Allow-Origin':process.env.ALLOWED_ORIGIN||'*','Access-Control-Allow-Headers':'Content-Type','Access-Control-Allow-Methods':'POST, OPTIONS'},body:JSON.stringify(data)});const bodyFrom=event=>typeof event.body==='string'?JSON.parse(event.body||'{}'):(event.body||event);const matches=(product,{category='',keyword='',tags=[]})=>{const text=`${product.name||''} ${product.category||''} ${product.brand||''} ${(product.tags||[]).join(' ')}`.toLowerCase();return(!category||product.category===category)&&(!keyword||text.includes(keyword.toLowerCase()))&&(!tags.length||tags.every(tag=>(product.tags||[]).includes(tag)))};exports.main=async event=>{try{if(event.httpMethod==='OPTIONS')return reply(204,'');const body=bodyFrom(event),limit=Math.min(Math.max(Number(body.limit)||12,1),50);const result=await db.collection('products').get();const products=(result.data||[]).filter(product=>matches(product,body)).slice(0,limit).map(product=>({...product,id:product.id||product._id}));console.log('[products] query result', {count:products.length,category:body.category||'',keyword:body.keyword||''});return reply(200,products)}catch(error){console.error('[products] failed',error.stack||error);return reply(500,{error:'Failed to load products',detail:error.message||String(error)})}};
