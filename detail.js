const id=new URLSearchParams(location.search).get('id');const product=window.productCatalog.find(item=>item.id===id)||window.productCatalog[0];const list=values=>`<ul>${values.map(value=>`<li>${value}</li>`).join('')}</ul>`;document.querySelector('#detail').innerHTML=`<article class="recommendation-card detail-card"><div class="result-image ${product.image}"></div><div class="result-main"><span class="result-type">${product.type}</span><h4>${product.name}</h4><strong class="result-price">${product.price}</strong><div class="result-reason"><b>AI 分析</b><p>${product.reason}</p></div><div class="result-details"><div><b>优点</b>${list(product.pros)}</div><div><b>缺点</b>${list(product.cons)}</div></div><p class="audience"><b>适合人群：</b>${product.people}</p><div class="result-reason"><b>购买建议</b><p>${product.advice}</p></div><div class="buy-links"><button data-store="京东">京东购买</button><button data-store="淘宝">淘宝购买</button></div></div></article>`;document.querySelectorAll('[data-store]').forEach(button=>button.addEventListener('click',()=>button.textContent=`${button.dataset.store}入口（模拟）`));
